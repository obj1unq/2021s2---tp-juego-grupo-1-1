import personajes.*
import inicio.*
import wollok.game.*

program juego {
	game.title("Pokemon-atrápalos ya!")
	game.height(13)
	game.width(20)
	game.cellSize(50)
	game.boardGround("mapaGrande.jpg")
	menu.iniciar()
	game.start()
} 